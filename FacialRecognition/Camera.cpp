#include "Camera.hpp"

Camera::Camera(void) {
	// only after we've hooked it up will we really know what we need in this class
}

bool Camera::takePhoto () {
	return false;
}

bool Camera::storePhoto (Photo p, char* filePath) {
	return false;
}
  
