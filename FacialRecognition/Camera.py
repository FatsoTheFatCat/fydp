# should Camera be grouped with facial recognition?
# or should it have its own folder?
import Photo

class Camera:
	def __init__(self):
		pass
    # TO DO: pick up the camera
    # only after we've hooked it up will we really know what we need in this class

  # def takePhoto():
  # def storePhoto(photo,filePath): ? should this be needed? hmm this might something that is done automatically or should be in photo handler
  